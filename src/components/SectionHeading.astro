---
// src/components/SectionHeading.astro
export interface Props {
  title: string;
  id?: string;
}

const { title, id } = Astro.props;
---
<h2 class="text-4xl font-extrabold font-heading text-center mb-12 text-brand-text" id={id}>
  {title}
</h2>